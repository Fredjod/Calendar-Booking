DROP TABLE IF EXISTS CALENDAR;

CREATE TABLE CALENDAR (
	week VARCHAR(7) NOT NULL,
	json CLOB (20K) NOT NULL,
	constraint CALENDAR_PK PRIMARY KEY (week));

INSERT INTO CALENDAR VALUES ('2019-31', load_file('2019-31.json', 'UTF-8') );

COMMIT;

SELECT * FROM CALENDAR;
